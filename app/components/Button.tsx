"use client";

import React from "react";
export default function AddButton() {
    return (
        <button
            onClick={() => {
                // do something
            }}
        >
            Add to cart
        </button>
    );
}